export const enum OPERATOR_TYPE {
    INSERT_MARKUP = 'insert',
    MOVE_EXISTING = 'move',
    REMOVE_NODE = 'remove',
    PROPS_CHANGE = 'props',
    TEXT_CHANGE = 'text'
}

export const enum PROP_TYPE {
    ADD,
    DELETE,
    MODIFY
}
